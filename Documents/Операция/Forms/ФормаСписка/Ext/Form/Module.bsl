
&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	Попытка
	
	Строка = Элементы.Список.ТекущиеДанные;	
	Если ЗначениеЗаполнено(Строка.Номер) Тогда
			Товары.Параметры.УстановитьЗначениеПараметра("Номер",Строка.Номер );  
		Иначе                                                                   
			Товары.Параметры.УстановитьЗначениеПараметра("Номер","" );
		КонецЕсли;
	Исключение
	КонецПопытки;
КонецПроцедуры
